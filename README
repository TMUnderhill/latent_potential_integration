# Latent potential: scRNA-seq, scATAC-seq integration code 

The custom scripts in this repository take as an input pre-processed scRNA-seq/scATAC-seq data in the form of a Seurat/Signac object with annotated clusters for both modalities, saved as `.RData` files. 

